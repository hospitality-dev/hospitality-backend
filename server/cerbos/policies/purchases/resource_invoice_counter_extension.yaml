apiVersion: api.cerbos.dev/v1
resourcePolicy:
  resource: purchases:invoice_counter_extension
  version: default
  rules:

  - actions: [ create, update ]
    effect: EFFECT_ALLOW
    roles: [ owner, manager ]

  - actions: [ view, list ]
    effect: EFFECT_ALLOW
    roles: [ '*' ]
