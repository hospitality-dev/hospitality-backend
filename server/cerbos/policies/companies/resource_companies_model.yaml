apiVersion: api.cerbos.dev/v1
resourcePolicy:
  resource: companies:model
  version: default
  rules:

  - actions: [ view, list, update, archive, delete ]
    effect: EFFECT_ALLOW
    roles: [ owner ]
